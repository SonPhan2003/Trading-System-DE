name: trading_system
version: "1.0.0"
config-version: 2

profile: trading_system

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  trading_system:
    silver:
      +materialized: table
    gold:
      +materialized: table
